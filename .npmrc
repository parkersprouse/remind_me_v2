# The global ~/.npmrc points @fortawesome at the Pro registry (npm.fontawesome.com),
# whose auth token currently 401s. This project only needs the free package.
@fortawesome:registry=https://registry.npmjs.org/
